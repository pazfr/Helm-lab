apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: service-applications
  annotations:
    description: "ArgoCD ApplicationSet for service deployments"

resources:
- applicationset/services-matrix-applicationset.yaml

commonLabels:
  app.kubernetes.io/name: service-applications
  app.kubernetes.io/part-of: argocd
  app.kubernetes.io/managed-by: kustomize

commonAnnotations:
  argocd.argoproj.io/sync-wave: "0"
