apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: client-onboarding-base

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - secret.yaml
  - scaledobject-keda.yaml

commonLabels:
  app: client-onboarding
  managed-by: kustomize
