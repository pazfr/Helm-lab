apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: facesplit-external-secret
  namespace: app
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: facesplit-secrets
    creationPolicy: Merge
    template:
      mergePolicy: Merge
  data:
  - secretKey: S3_ACCESS_KEY
    remoteRef:
      key: S3_KEYS
      property: ACCESS_KEY
  - secretKey: S3_SECRET_KEY
    remoteRef:
      key: S3_KEYS
      property: SECRET_KEY
  - secretKey: DATABASE_PASS
    remoteRef:
      key: rds_credentials_dev
      property: password
  - secretKey: DATABASE_URL
    remoteRef:
      key: rds_credentials_dev
      property: QA_DATABASE_URL
  - secretKey: DATABASE_USERNAME
    remoteRef:
      key: rds_credentials_dev
      property: username
  - secretKey: SSL_KEY
    remoteRef:
      key: qa-internal-services-ssl-key
  - secretKey: SSL_CERT
    remoteRef:
      key: qa-internal-services-ssl-cert
  - secretKey: SSL_KEY_PASSWORD
    remoteRef:
      key: qa-internal-services-ssl-key-password
