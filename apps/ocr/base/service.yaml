apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  namespace: app
  labels:
    app: ocr
spec:
  type: ClusterIP
  ports:
  - name: 5040-tcp
    port: 5040
    targetPort: 5040
    protocol: TCP
  selector:
    app: ocr
