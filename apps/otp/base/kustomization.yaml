apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: otp-base

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
- secret.yaml
- scaledobject-keda.yaml

commonLabels:
  app: otp
  managed-by: kustomize
