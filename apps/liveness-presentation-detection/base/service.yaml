apiVersion: v1
kind: Service
metadata:
  name: liveness-presentation-detection-service
  namespace: app
  labels:
    app: liveness-presentation-detection
spec:
  type: ClusterIP
  ports:
  - name: pre-8443-tcp
    port: 8443
    targetPort: 8443
    protocol: TCP
  selector:
    app: liveness-presentation-detection
