apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: liveness-presentation-detection-ingress
  annotations:
    alb.ingress.kubernetes.io/group.name: qa-internal
    alb.ingress.kubernetes.io/load-balancer-name: qa-eks-internal-alb
    external-dns.alpha.kubernetes.io/hostname: liveness-presentation-detection.qa-eks.internal-services

spec:
  rules:
  - host: liveness-presentation-detection.qa-eks.internal-services
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: liveness-presentation-detection-service
            port:
              number: 8443
