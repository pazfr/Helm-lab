image:
  tag: idface-server-prod-1.49.2

service:
  labels:
    version: ""

config:
  data:
    SERVER_ALLOW_CORS: "true"
    SERVER_SSL_ENABLED: "true"

ingress:
  annotations:
    alb.ingress.kubernetes.io/group.name: dev-internal
    alb.ingress.kubernetes.io/load-balancer-name: dev-eks-internal-alb
    

externalSecrets:
  - name: liveness-presentation-detection-external-secret
    secretStoreRef:
      name: aws-secrets-manager
      kind: ClusterSecretStore
    refreshInterval: 5m
    targetName: liveness-presentation-detection-secrets
    data:
      - secretKey: SSL_KEY
        remoteRef:
          key: dev-eks-internal-services-ssl-key
      - secretKey: SSL_CERT
        remoteRef:
          key: dev-eks-internal-services-ssl-cert
      - secretKey: SSL_KEY_PASSWORD
        remoteRef:
          key: dev-eks-internal-services-ssl-key-password
  
