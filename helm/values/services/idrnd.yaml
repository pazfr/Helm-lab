replicaCount: 2
service:
  name: idrnd
  port: 8443
  targetPort: 8443
  ports:
  - name: 8443-tcp
    port: 8443
    targetPort: 8443
    protocol: TCP
  

ingress:
  internal: true
  className: alb
  annotations:
    alb.ingress.kubernetes.io/backend-protocol: HTTPS
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 8443}]'
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/target-type: ip
    external-dns.alpha.kubernetes.io/ttl: "300"
    alb.ingress.kubernetes.io/healthcheck-path: /api_version
    alb.ingress.kubernetes.io/healthcheck-port: '8443'
    alb.ingress.kubernetes.io/healthcheck-protocol: HTTPS
    
  paths:
    - path: /
      pathType: Prefix
      serviceName: idrnd-service
      port: 8443
    

image:
  repository: 495947449196.dkr.ecr.eu-central-1.amazonaws.com/idrnd
  tag: latest

resources:
  requests:
    cpu: "1024m"
    memory: "5Gi"
  limits:
    cpu: "3600m"
    memory: "7Gi"
  

config:
  create: true
  name: idrnd-config
  data: {}

secret:
  create: false
  name: idrnd-secrets

externalSecrets: []

scaledObject:
  enabled: true
  pollingInterval: 30
  cooldownPeriod: 300
  minReplicaCount: 2
  maxReplicaCount: 3
  triggers:
    - type: cpu
      metadata:
        type: Utilization
        value: "60"
    - type: memory
      metadata:
        type: Utilization
        value: "70"
